{
  "name": "@openteam/worker",
  "version": "0.0.0",
  "license": "GPL-3.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts src/healthcheck.ts --outdir dist --target node --format esm --external pg-native",
    "dev": "bun --watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json",
    "test": "bun test --pass-with-no-tests"
  },
  "dependencies": {
    "@openteam/contracts": "workspace:*",
    "@openteam/db": "workspace:*",
    "@openteam/messaging": "workspace:*",
    "effect": "3.22.1",
    "pg-boss": "12.28.0",
    "@openteam/plugin-sdk": "workspace:*"
  }
}
